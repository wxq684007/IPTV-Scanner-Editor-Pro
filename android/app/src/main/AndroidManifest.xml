<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<!-- 音频可视化频谱采集（AudioRecord + Visualizer 需要 RECORD_AUDIO） -->
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<!-- 音频可视化音量控制（Visualizer 需要 MODIFY_AUDIO_SETTINGS） -->
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- 应用内 APK 安装所需权限（Android 8+）：
         用于发现新版本后直接通过 DownloadManager 下载并调起安装 Intent，
         避免 TV 端跳转浏览器不便的问题 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 所有文件访问权限（Android 11+ scoped storage 下写入 /sdcard/ISEP/ 需要）：
         TV 端用户需在 系统设置 > 应用 > ISEP > 权限 > 所有文件访问权限 中手动授予。
         未授予时数据回退到 getExternalFilesDir()/ISEP/（覆盖安装不丢失，但文件管理器不可见）。
         安全建议：仅在用户主动授予权限时使用，不主动弹出请求。数据目录优先使用
         app-specific storage（getExternalFilesDir），仅在用户明确需要跨应用文件共享时
         才使用 MANAGE_EXTERNAL_STORAGE。 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- 开机自启动权限（与酷9 BOOT_START 对齐）：
         接收 BOOT_COMPLETED 广播，在用户开启"开机自启动"设置后自动启动应用。
         面向 IPTV 盒子/电视场景，不在 Play Store 分发。 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- TV 端支持声明（required=false 让应用同时可在手机和 TV 上安装）：
         - leanback: Android TV 特性（D-pad 导航、TV UI 模式）
         - touchscreen 不强制要求：TV 端无触摸屏，需声明 false 才能在 TV 上安装 -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".IptvApplication"
        android:allowBackup="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IPTVScanner"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Compose 主入口 Activity（LAUNCHER）
             替换原 Java+WebView 三层架构，改为 Jetpack Compose 原生 UI + Chaquopy 直调 Python -->
        <activity
            android:name=".MainActivityCompose"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 文件关联：打开 M3U/M3U8 播放列表 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:mimeType="application/x-mpegurl" />
                <data android:mimeType="application/vnd.apple.mpegurl" />
                <data android:mimeType="audio/x-mpegurl" />
                <data android:mimeType="video/x-mpegurl" />
            </intent-filter>
            <!-- 文件关联：打开视频文件 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <!-- 文件关联：通过文件扩展名打开 M3U -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.m3u" />
                <data android:pathPattern=".*\\.m3u8" />
            </intent-filter>
        </activity>

        <!-- 开机自启动接收器（与酷9 BOOT_START 对齐） -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- FileProvider：用于应用内 APK 更新，把下载好的 APK 通过 content:// URI 分享给系统安装器 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>